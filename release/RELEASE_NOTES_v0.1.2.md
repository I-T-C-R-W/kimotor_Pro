# KMotor_Pro v0.1.3

Package metadata fix release.

## Fixes

- Adjusted the PCM `license` field to a value accepted by KiCad 9.
- Fixes package metadata parsing during `Install from File`.
- Keeps the same application feature set as `v0.1.2`.
