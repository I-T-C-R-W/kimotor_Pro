# KMotor_Pro v0.1.2

Packaging fix release.

## Fixes

- Rebuilt the KiCad package in proper PCM format:
  - `metadata.json` at archive root
  - `plugins/` subdirectory for plugin files
  - `resources/icon.png` for PCM display
- Added PCM-compatible package metadata for `Install from File`.
- Keeps the same application feature set as `v0.1.1`.
